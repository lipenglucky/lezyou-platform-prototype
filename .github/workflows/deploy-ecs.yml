# push 到 main 分支 → 自动部署到阿里云 ECS
# GitHub Secrets：ECS_HOST、ECS_USER、ECS_SSH_KEY

name: Deploy to ECS

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-ecs
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check secrets
        run: |
          test -n "${{ secrets.ECS_HOST }}" || (echo "缺少 Secret: ECS_HOST" && exit 1)
          test -n "${{ secrets.ECS_USER }}" || (echo "缺少 Secret: ECS_USER" && exit 1)
          test -n "${{ secrets.ECS_SSH_KEY }}" || (echo "缺少 Secret: ECS_SSH_KEY" && exit 1)
          echo "Secrets 已配置 ✓"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Pack source
        run: |
          tar czf deploy.tar.gz \
            --exclude=node_modules \
            --exclude=.next \
            --exclude=prisma/dev.db \
            --exclude=prisma/dev.db-journal \
            --exclude=.git \
            --exclude=.env \
            --exclude=backups \
            --exclude=deploy.tar.gz \
            .
          ls -lh deploy.tar.gz

      - name: Upload to ECS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.ECS_HOST }}
          username: ${{ secrets.ECS_USER }}
          key: ${{ secrets.ECS_SSH_KEY }}
          port: 22
          source: deploy.tar.gz
          target: /opt/lezyou
          overwrite: true

      - name: Extract and deploy
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.ECS_HOST }}
          username: ${{ secrets.ECS_USER }}
          key: ${{ secrets.ECS_SSH_KEY }}
          port: 22
          command_timeout: 25m
          script: |
            set -e
            cd /opt/lezyou
            if [ -f .env ]; then cp -f .env /tmp/lezyou.env.bak; fi
            tar xzf deploy.tar.gz
            if [ -f /tmp/lezyou.env.bak ]; then cp -f /tmp/lezyou.env.bak .env; fi
            rm -f deploy.tar.gz
            chmod +x deploy/*.sh scripts/cron-order-timeouts.sh 2>/dev/null || true
            bash deploy/remote-deploy.sh
